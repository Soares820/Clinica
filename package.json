{
  "name": "studio-aura",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de agendamento, loja e gestão do Studio Aura",
  "scripts": {
    "build": "esbuild entry.jsx --bundle --minify --format=iife --outfile=docs/bundle.js"
  },
  "dependencies": {
    "lucide-react": "^1.31.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
