{
  "name": "studio-aura",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de agendamento, loja e gestão da Pharus Estética & Spa",
  "scripts": {
    "config:from-env": "node scripts/gerar-config.js",
    "build": "npm run config:from-env && esbuild entry.jsx --bundle --minify --format=iife --outfile=docs/bundle.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "lucide-react": "^1.31.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.7",
    "esbuild": "^0.28.2",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
